{
  "name": "gestion-tareas-nico",
  "version": "1.0.0",
  "description": "Sistema de gestión de tareas con metodología Scrum",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../tests && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "test:e2e": "cd tests && npm run test:e2e"
  },
  "keywords": ["scrum", "task-management", "react", "nodejs"],
  "author": "NICO",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
